import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
  iconImage: {
    height: 16,
    width: 16,
    marginHorizontal: 5,
  },
});

export default styles;
